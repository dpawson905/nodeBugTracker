// Modal
#bugModal.modal.fade(tabindex='-1' aria-labelledby='bugModalLabel' aria-hidden='true')
  .modal-dialog
    .modal-content
      .modal-header
        h5#bugModalLabel.modal-title.capitalize Submit a bug
        button.close(type='button' data-dismiss='modal' aria-label='Close')
          span(aria-hidden='true') &times;
      .modal-body
        form(action=`/projects/${project.id}/bug?_csrf=${csrfToken}`, method="post" enctype="multipart/form-data")
          .form-group
            label.capitalize(for="title") Bug title
            input.form-control(type="text" name="title" id="title" placeholder="Bug Title" autofocus required)
          .form-group
            label.capitalize(for="description") Describe the issue?
            textarea.form-control(name="description" id="description" placeholder="Describe the issue as best as you can" rows="5" required)
          .form-group
            label(for="image") Screenshot if applicable
            input.form-control-file(type="file", name="image")
          .modal-footer
            input(type="hidden", name="_csrf" value=`${csrfToken}`)
            button.btn.btn-secondary(type='button' data-dismiss='modal') Close
            button.btn.btn-primary(type='submut') Submit